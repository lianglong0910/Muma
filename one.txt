git status 查看本地仓库状态     master
git add .  提交到暂存区
git commit -m 'xxx'  讲暂存区提交到本地版本库


git checkout lhj        切换分支
git branch  //查看当前分支

git pull origin lhj

git status
git add .
git commit -m 'xxx'

git merge master    master合并到当前分支

git status
git add .
git commit -m 'xxx'

git push origin lhj

git checkout master

git merge lhj  //合并

git status
git add .
git commit -m 'xxx'

git pull origin master

git push origin master


